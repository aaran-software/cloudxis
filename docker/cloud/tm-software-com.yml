services:
  tm_software_com:
    image: codexsun:v4
    container_name: tm_software_com
    ports:
      - "8000:8000"
    volumes:
      - ../../apps/devops/cloud:/home/devops/cloud
    networks:
      - codexion-network
    restart: unless-stopped
    tty: true

networks:
  codexion-network:
    external: true