services:
  aaranwebs:
    image: codexion:v5
    container_name: aaranwebs
    ports:
      - "8080:80"
      - "4443:443/tcp"
      - "4443:443/udp"
      - "7021:80"
    volumes:
      - ../../../cloud/aaranwebs:/app
    networks:
      - codexion-network
    restart: unless-stopped
    environment:
      APP_ENV: local
      APP_DEBUG: "false"
    tty: true

networks:
  codexion-network:
    external: true